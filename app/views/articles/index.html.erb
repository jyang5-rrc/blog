<h1>Articles</h1>

<ul>
  <% @articles.each do |article| %>
    <li>
      <%# <%= article.title %> <!--The <%= %> tag means "evaluate the enclosed Ruby code, and output the value it returns.-->
      <%# <a href="/articles/<%= article.id %>
      <%# <a href="<%= article_path(article) %>
        <%# <%= article.title %>
      <%# </a> %>
      <%= link_to article.title, article %> <!--article as second argum,article_path(article) is the same as article-->
    </li>
  <% end %>
</ul>


<%= link_to "New Article", new_article_path %> <!--new_article_path is the link of new.html.erb-->